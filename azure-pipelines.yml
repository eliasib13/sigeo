# PHP
# Test and package your PHP project.
# Add steps that run tests, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/php

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
  phpVersion: 7.2

steps:
- script: |
    sudo update-alternatives --set php /usr/bin/php$(phpVersion)
    sudo update-alternatives --set phar /usr/bin/phar$(phpVersion)
    sudo update-alternatives --set phpdbg /usr/bin/phpdbg$(phpVersion)
    sudo update-alternatives --set php-cgi /usr/bin/php-cgi$(phpVersion)
    sudo update-alternatives --set phar.phar /usr/bin/phar.phar$(phpVersion)
    php -version
  displayName: 'Use PHP version $(phpVersion)'

- script: sudo apt-get install mysql-client
  displayName: 'Installing MySQL'

- script: mysql -e 'create database sigeo;'
  displayName: 'Creating SiGEO database'

- script: composer install --no-interaction --prefer-dist
  displayName: 'composer install'

- script: chmod -R 777 storage/

- script: |
    php artisan migrate --env=testing --no-interaction -vvv
    php artisan db:seed --env=testing --no-interaction -vvv
    vendor/bin/phpunit
    php artisan migrate:rollback --env=testing --no-interaction -vvv
  displayName: Running DB migrations and seeding + unit testing

